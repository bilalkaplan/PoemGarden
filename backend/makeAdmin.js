require('dotenv').config(); const mongoose = require('mongoose'); const User = require('./models/User'); mongoose.connect(process.env.MONGO_URI).then(async () => { const user = await User.findOneAndUpdate({ email: 'bilalkapl4n@gmail.com' }, { role: 'admin' }, { returnDocument: 'after' }); console.log(user ? 'User ' + user.nickname + ' is now admin.' : 'User not found.'); process.exit(0); }).catch(err => console.error(err));
